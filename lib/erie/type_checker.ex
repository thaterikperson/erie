defmodule Erie.TypeChecker do
  def check(translator) do
    # IO.inspect(translator)
    :ok
  end

  # def last_line_in_function({:function, _, _, _, [clause]}) do
  #   {:clause, _, _, _, lines}
  #   List.last(lines)
  # end
end
